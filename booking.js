document.addEventListener('DOMContentLoaded',()=>{const catalog=document.querySelector('#bookingServices');const steps=[...document.querySelectorAll('.booking-step')];const form=document.querySelector('#bookingForm');let current=1;let selected=null;const show=n=>{current=n;steps.forEach(s=>s.classList.toggle('active',Number(s.dataset.step)===n));document.querySelectorAll('.booking-progress span').forEach((s,i)=>s.classList.toggle('active',i<n));window.scrollTo({top:document.querySelector('.booking-shell').offsetTop-100,behavior:'smooth'})};if(catalog&&Array.isArray(window.chamomileServices)){const categories=[...new Set(window.chamomileServices.map(s=>s.category))];catalog.innerHTML=categories.map(category=>{const items=window.chamomileServices.filter(s=>s.category===category).slice(0,8);return `<section class="booking-category"><div class="category-title"><span>${category}</span><small>PRICE · N/A</small></div><div class="booking-service-grid">${items.map((s,i)=>`<button type="button" class="booking-service" data-service="${String(s.name).replace(/"/g,'&quot;')}"><span>${String(i+1).padStart(2,'0')}</span><strong>${s.name}</strong><small>${s.duration||'Duration N/A'} · N/A</small></button>`).join('')}</div></section>`}).join('');catalog.querySelectorAll('.booking-service').forEach(btn=>btn.addEventListener('click',()=>{catalog.querySelectorAll('.booking-service').forEach(b=>b.classList.remove('selected'));btn.classList.add('selected');selected=btn.dataset.service}))}document.querySelectorAll('[data-next]').forEach(btn=>btn.addEventListener('click',()=>{if(current===1&&!selected){alert('Please choose a service first.');return}if(current===3){const date=document.querySelector('#date'),time=document.querySelector('#time');if(!date.reportValidity()||!time.reportValidity())return}show(Number(btn.dataset.next))}));document.querySelectorAll('[data-prev]').forEach(btn=>btn.addEventListener('click',()=>show(Number(btn.dataset.prev))));document.querySelectorAll('.choice').forEach(c=>c.addEventListener('click',()=>{document.querySelectorAll('.choice').forEach(x=>x.classList.remove('selected'));c.classList.add('selected')}));const date=document.querySelector('#date');if(date){const d=new Date();d.setMinutes(d.getMinutes()-d.getTimezoneOffset());date.min=d.toISOString().split('T')[0]}form?.addEventListener('submit',e=>{e.preventDefault();const name=form.elements.name.value.trim();if(!name)return;form.hidden=true;document.querySelector('.booking-progress').hidden=true;const success=document.querySelector('#bookingSuccess');document.querySelector('#successName').textContent=name;success.hidden=false;window.scrollTo({top:0,behavior:'smooth'})})});